{
  "name": "catawiki-price-calculator",
  "version": "0.1.0",
  "description": "Browser extension that shows the final total price on Catawiki auction pages",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
